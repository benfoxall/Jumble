<% if UserSession.find %>
	<em><%=h UserSession.find.user.name %></em> 
	<%= link_to 'logout', logout_path, :class => 'return' %>
<% else %>
	<%= link_to 'login', login_path, :class => 'return' %>
<% end %>