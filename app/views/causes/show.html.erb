<p>
	<%= link_to "Sell something", sell_path(:cause_id => params[:id]), :class => 'awesome beta right' %>
</p>

<h1 id="cause-title">
	<%=h @cause.title %>
</h1>

<div id="description">
	<%= simple_format h(@cause.description) %>
	<% if owner? && @cause.description.blank? %><p>Add a description</p><% end %>
</div>

<div class="hr">
	<hr />
</div>

<div id="items">
	<%= render @cause.items %>
</div>
